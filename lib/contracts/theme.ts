export type CrtTheme = {
  bg: string;
  default: {
    color: string;
    glow: string;
  };
  warning: {
    color: string;
    glow: string;
  };
  error: {
    color: string;
    glow: string;
  };
};